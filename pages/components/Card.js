const Card = () => {
  return (
    <div className={`card`}>
      <div className={`card-text`}>
        <h1 className={`card-title`}>DESESCOLARIZAÇÃO DA SOCIEDADE</h1>
        <h3 className={`card-description`}>
          Um dos grandes mitos da nossa época está na crescente
          institucionalização
        </h3>
        <h2 className={`card-signature`}>- Ivan Illich</h2>
      </div>
    </div>
  );
};

export default Card;
